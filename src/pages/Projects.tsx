import { Folder } from 'lucide-react';
import ComingSoon from '../components/common/ComingSoon';

export default function Projects() {
  return <ComingSoon moduleName="个人项目" icon={<Folder size={64} />} />;
}
